{
    # Global options
    debug  # Optional: helpful during initial setup
    servers {
        protocol {
            experimental_http3
        }
    }
}

:{$PORT} {
    # Simple TCP forwarding
    tee {
        upstream tcp://{$CLICKHOUSE_HOST}:{$CLICKHOUSE_PORT}
    }
}